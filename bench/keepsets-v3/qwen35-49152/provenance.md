# Provenance: keep-set v3 -- qwen35-49152

Generated by `bench/emit-keepset-v3.py` at script commit `711b55e38523254dc9be21f7f83618c0d6d3f967`, using strategy **A-fixed** (v1's merge-rank method, corrected -- see below).

## Exact command line

```
python3 bench/emit-keepset-v3.py --tokenizer /Users/oleh/.cache/huggingface/hub/models--mlc-ai--Qwen3.5-0.8B-q4f16_1-MLC/snapshots/0ec138972555613c1d7812a821778ad0398c8790/tokenizer.json --tokenizer-label qwen35 --target-size 49152
```

## Selection method

Strategy A (merge-rank selection, `bench/vocab-coverage.py`'s `build_keep_set_for_tokenizer` + `final_keep_set_for_K`, v1's method verbatim: corpus-seen token ids from bench-data.json, bench-data-extended.json, pdf-pastes.json, arxiv-pastes.json, synth-spans.jsonl latex rows, unioned with byte alphabet + added/special tokens + webnn-workbench harness-prompt tokens + KaTeX macro seed + maths-English word seed, padded to K by ascending original token id) won the v3 fertility sweep at every K for both tokenizers, by a wide margin over strategies B and C (see `bench/README-keepset-v3.md`). But running the brief's Job 4 validation checks against pure strategy A found it FAILS check 1 for the Qwen3.5 tokenizer: it reproduces v1's own originally-reported failure (the 7 named LatexGen-prompt token ids, `bench/validate-keepset-v2.py`'s `QWEN35_FAILURE1_IDS`, are absent), because v1's corpus never included LatexGen's actual client prompt text (`public/pipeline.js` / `public/pdf-prompt.js`) -- only the webnn-workbench harness's separate, narrower prompt set. **A-fixed** = strategy A's exact base, unioned with the same LatexGen-prompt token layer B/C use (`bench/build-keepset-v2.py`'s `latexgen_and_harness_prompt_ids`, itself reusing `bench/build-keepset-seed-latexgen-prompts.py`'s seed, not re-derived), then BPE merge-ancestor-closed over the combined base (`bench/build-keepset-v2.py`'s `compute_merge_ancestor_closure`, reused) so no kept corpus/prompt token's producing merge rule gets silently dropped -- v1 never did this closure step at all, which is a real, independent defect (a corpus-seen multi-character token can still lose its OWN merge rule if an ancestor piece isn't independently kept), not specific to the prompt-token fix. Padded to K exactly like A, by ascending original token id. This is a minimal, justified patch to the winning strategy to pass the brief's own validation gate, not a re-tuned number: see `bench/README-keepset-v3.md`'s "Job 4 validation" section for the full before/after.

## Why this K (49152)

Leakage-free disjoint-corpus check (bench/README-keepset-v3.md 'Is strategy A's win real, or leakage?') gives per-model-size optima of K=24576 (0.8B, 4B) and K=65536 (9B) for strategy A-fixed on this tokenizer -- the smaller 0.8B/4B models spend a larger head-kernel share of decode time, so they benefit more from pruning; the 9B model's head share is small enough that the fertility cost of aggressive pruning outweighs the saved compute sooner. K=32768 is emitted as the primary recommendation: a deliberate compromise that is within ~0.3% net-speedup of each model's own leakage-free optimum (see README table), plus one K either side (24576, 49152) from the sweep grid per the brief.

- target K: 49152, actual size achieved: 49152, clipped to natural: False
- natural (no-padding) keep-set size for this tokenizer: 7955
- BPE merges: 247587 -> 48870 (dropped 198717, 0.8026)
- length check: expected K=49152, tokenizers.Tokenizer.from_file=49152, transformers.AutoTokenizer.from_pretrained=49152
- v3-methodology fertility (426 held-out texts: 120 arxiv-pastes.json input+reference + 93 bench-data-extended.json ok items input+reference, real re-encode): before=0.2599, after=0.2599, inflation=+0.0000%
- stop_token_id mismatches found and corrected: [{"index": 0, "declared_stop_token_id": 151643, "stop_str": "<|endoftext|>", "token_at_declared_id": "\u0120\u00eb\u0124\u00b4\u00ec\u013c\u00a9", "corrected_old_id": 248044}, {"index": 1, "declared_stop_token_id": 151645, "stop_str": "<|im_end|>", "token_at_declared_id": "\u00e0\u00b8\u00ab\u00e0\u00b8\u013b\u00e0\u00b8\u00b1\u00e0\u00b8\u0129", "corrected_old_id": 248046}]

See `SHA256SUMS.txt` in this directory for the authoritative, current SHA-256 of every emitted file (including this one).

