{
  "name": "latexgen",
  "version": "0.1.0",
  "private": true,
  "description": "Text and screenshots to validated LaTeX, running in the browser.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.mjs",
    "bench:images": "python3 bench/render-images.py"
  },
  "engines": { "node": ">=20" }
}
